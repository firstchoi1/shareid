# 新增站点操作

## 当前架构

- 所有站点共用同一个代码仓库。
- 所有站点共用同一个 PostgreSQL 容器：`shareid-postgres`。
- 所有站点的站点配置、分类标签、兑换码都存数据库。
- GitHub Actions 优先读取服务器本地部署清单：`/etc/shareid/sites.txt`。

## 新增站点前提

服务器上需要满足：

1. `/var/www/shareid` 已经是最新代码。
2. `shareid-postgres` 数据库容器已运行。
3. `/etc/caddy/Caddyfile` 可以正常 `reload`。
4. 参考站点 `.env` 里已有可用的：
   - `DATABASE_URL`
   - `SHOWCASE_APPLE_AUTO_BASE_URL`
   - `SHOWCASE_APPLE_AUTO_API_KEY`

新增脚本会复制参考站点的 `.env`，然后覆盖：

- `SITE_KEY`
- `SHAREID_ADMIN_PASSWORD`

## 一键新增站点

先更新参考站点代码：

```bash
cd /var/www/shareid
git pull origin main
```

然后执行：

```bash
bash scripts/add-site.sh --domain https://www.example.store/ --admin-password '你的后台密码'
```

示例：

```bash
bash scripts/add-site.sh --domain https://www.abcid.store/ --admin-password 'Abcid2026@admin'
```

## 先预演，不落地

如果你想先确认脚本会做什么，再真正执行，用：

```bash
bash scripts/add-site.sh --domain https://www.abcid.store/ --admin-password 'Abcid2026@admin' --dry-run
```

`--dry-run` 会打印：

- 推导出的 `site_key`
- 计划使用的目录和端口
- 计划写入的 Caddy 片段
- 计划执行的数据库和部署命令

但不会真正修改服务器状态。

## 脚本自动完成的事情

脚本会自动：

1. 从域名推导 `site_key`
   - `https://www.abcid.store/` -> `abcid`
2. 自动分配未占用端口
3. 创建站点目录
   - `/var/www/abcid`
4. clone 同一个仓库
5. 复制参考站点 `.env`
6. 自动写入：
   - `SITE_KEY=abcid`
   - `SHAREID_ADMIN_PASSWORD=...`
7. 在数据库中创建新站点
8. 从参考站点复制数据库中的：
   - `sites` 基础配置
   - `site_regions` 分类标签配置
9. 将新站注册到：
   - `/etc/shareid/sites.txt`
10. 自动追加 Caddy 配置
11. 校验并 reload Caddy
12. 首次执行部署并启动 PM2

## 执行完成后的输出

脚本结束时会打印：

```text
site_key=abcid
app_name=abcid
app_dir=/var/www/abcid
app_port=3007
domain=https://www.abcid.store
```

如果是预演模式，还会额外打印：

```text
mode=dry-run
```

## 失败时怎么看

脚本失败时会明确打印：

- 失败阶段
- 已经完成的动作
- 建议的人工回滚命令

常见回滚提示会覆盖：

- 删除刚 clone 的新目录
- 从 `/etc/shareid/sites.txt` 删除新增行
- 从 `/etc/caddy/Caddyfile` 删除刚追加的域名配置
- 从数据库删除该 `site_key` 的站点记录

这一步是提示，不会自动删你服务器上的内容。

## 新增后检查

### 1. 检查 PM2

```bash
pm2 list
```

### 2. 检查数据库

```bash
docker exec -i shareid-postgres psql -U shareid_user -d shareid -c "SELECT id, site_key, site_name, domain FROM sites ORDER BY id;"
```

### 3. 检查部署清单

```bash
cat /etc/shareid/sites.txt
```

### 4. 检查 Caddy 配置是否已追加

```bash
grep -n "abcid.store" /etc/caddy/Caddyfile
```

### 5. 访问后台

```text
https://www.abcid.store/admin
```

## 新站初始化后需要手工做的事

脚本只会复制参考站点的数据库配置，不会猜新站自己的标签 ID。

所以新增后你仍然需要进入该站后台，调整：

1. 购买链接
2. 分类标签 ID
3. 国家备注
4. 托管接口地址和 API Key（如果新站不同）
5. 是否开启兑换模式

## 自动部署说明

新增成功后，不需要再改仓库里的 `deploy/sites.txt`。

后续 GitHub Actions 会优先读取服务器本地：

```text
/etc/shareid/sites.txt
```

所以这个新站会自动加入后续多站部署。

## 常见问题

### 1. 域名已经有配置，脚本还能重复执行吗？

可以。

脚本会检测已存在的 `site_key`，已存在时复用现有目录和端口。

### 2. 新站为什么默认复制了旧站配置？

这是设计行为。

你当前只输入域名和后台密码，脚本无法自动推断新站的标签 ID，所以先复制参考站点配置，再让你进后台调整。

### 3. 新站为什么没有兑换码？

脚本不会复制旧站兑换码。

新站兑换码默认从空开始。
